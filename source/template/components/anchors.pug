-
  var anchors = [
    ['Самое важное в SASS'],
    ['Приёмы в вёрстке, без которых не обходится ни один сайт'],
    ['Самый необходимый набор Gulp плагинов'],
    ['Почему я выбрал Jade']
  ]

ul.anchors
  each anchor, ndx in anchors
    if ndx == 0
      li.anchors__item.anchors__item_active=anchor[0]
    else
      li.anchors__item=anchor[0]
span.toogle-anchors